import { Command } from "https://raw.githubusercontent.com/sanihaq/deno-cliffy/master/command.ts";

export class CommonCommands extends Command {
  constructor() {
    super();
    // this.option("-d, --debug", "show debug info");
  }
}
